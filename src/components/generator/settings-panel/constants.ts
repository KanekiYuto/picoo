// 焦点环样式常量
export const FOCUS_RING_CLASSES =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/60 focus-visible:ring-offset-2 focus-visible:ring-offset-background";

// 按钮过渡效果样式
export const BUTTON_TRANSITION_CLASSES = "transition-colors cursor-pointer";

// 卡片背景样式
export const CARD_BG_CLASSES = "bg-[var(--color-generator-panel-card-bg)]";

// 比例预览最大尺寸
export const MAX_PREVIEW_SIZE = 160;

// 业务常量
export const VARIATION_OPTIONS = [1, 2, 3, 4] as const;
export const VISIBILITY_OPTIONS = ["public", "private"] as const;
